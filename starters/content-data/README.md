# {{APP_TITLE}}

Content/data Hao app starter: Astro + TypeScript + Tailwind.

## Run

```bash
npm install
npm run dev
```

After the first install, commit the generated lockfile. Keep canonical content/data explicit and build user-facing pages as projections of that authority.
