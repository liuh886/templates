# {{APP_TITLE}}

Interactive Hao app starter: React + TypeScript + Vite.

## Run

```bash
npm install
npm run dev
```

After the first install, commit the generated lockfile. Replace the neutral placeholder with the real vertical slice from `PRODUCT.md`; remove unused starter code instead of preserving it.
