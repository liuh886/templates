# {{APP_TITLE}}

Minimal Hao app starter: browser-native HTML, CSS and JavaScript with no framework or build step.

## Run

Serve the directory with any static server, for example:

```bash
python3 -m http.server 8000
```

Then replace the neutral placeholder with the real core loop described in `PRODUCT.md`.
